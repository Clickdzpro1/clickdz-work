# ClickDz Work Railway Environment Variables
# Paste these into Railway Dashboard → Variables

# Database (Supabase session pooler, port 5432)
DATABASE_URL=postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres

# Redis (Railway auto-provides these after adding Redis service)
REDIS_SERVER_HOST=${{Redis.RAILWAY_PRIVATE_DOMAIN}}
REDIS_SERVER_PORT=6379
REDIS_SERVER_PASSWORD=${{Redis.REDIS_PASSWORD}}

# Server config
AFFINE_SERVER_HTTPS=true
AFFINE_SERVER_HOST=work.clickdz.ai
AFFINE_SERVER_PORT=8080

# AI features (OpenAI)
COPILOT_OPENAI_API_KEY=sk-your-openai-key-here

# Feature flags
AFFINE_INDEXER_ENABLED=true

# Optional: Mailer (for email notifications)
# MAILER_HOST=smtp.gmail.com
# MAILER_PORT=587
# MAILER_USER=your-email@gmail.com
# MAILER_PASSWORD=your-app-password
# MAILER_SENDER=ClickDz Work <noreply@clickdz.ai>

# Optional: OAuth (Google/GitHub login)
# OAUTH_GOOGLE_CLIENT_ID=your-google-client-id
# OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Optional: Storage (S3/CloudFront for file uploads)
# STORAGE_PROVIDER=s3
# STORAGE_S3_BUCKET=clickdz-work-uploads
# STORAGE_S3_REGION=us-east-1
# STORAGE_S3_ACCESS_KEY=your-access-key
# STORAGE_S3_SECRET_KEY=your-secret-key

# Optional: Sentry (error tracking)
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
